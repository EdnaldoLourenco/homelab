apiVersion: v1
kind: Service
metadata:
  labels:
    app: livechat
    will: livechat
  name: livechat
  namespace: apps
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: livechat

